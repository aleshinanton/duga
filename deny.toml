# deny.toml
# Populated in EPIC-15: Build + CI

[lints.deny]
elided_lifetimes_in_paths = "warn"
unreachable_pub = "warn"
irrefutable_let_patterns = "warn"
unsafe_op_in_unsafe_fn = "warn"

[lints.warn]
missing_docs = "warn"