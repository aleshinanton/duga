[package]
name = "duga-plugin-abi"
version = "0.1.0"
edition = "2021"
rust-version = "1.77"
description = "WIT contract and shared plugin ABI types for duga"
license = "MIT"

[features]
default = []
host = []
guest = []

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
schemars = "0.8"

